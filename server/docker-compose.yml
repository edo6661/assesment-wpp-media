services:
  app:
    build: .
    container_name: assesment-wpp
    restart: unless-stopped
    env_file: .env
    environment:
      - TZ=Asia/Jakarta
    depends_on:
      - db
    networks:
      - proxy-network
      - assesment-internal

  db:
    image: mysql:8.0
    container_name: assesment-wpp-db
    command: --skip-name-resolve --default-authentication-plugin=mysql_native_password
    restart: unless-stopped
    ports:
      - "127.0.0.1:3307:3306"
    environment:
      MYSQL_DATABASE: ${DB_NAME}
      MYSQL_USER: ${DB_USER}
      MYSQL_PASSWORD: ${DB_PASSWORD}
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
      TZ: Asia/Jakarta
    volumes:
      - assesment_db_data:/var/lib/mysql
    networks:
      - assesment-internal

networks:
  proxy-network:
    external: true
  kavling-internal:
    driver: bridge

volumes:
  kavling_db_data:
